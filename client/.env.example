# Where the UGC backend (Fastify) is running. Copy this file to `.env`.
#
# Local dev:
VITE_API_URL=http://localhost:3001
#
# Droplet (no TLS — use the droplet IP and the published port):
# VITE_API_URL=http://<droplet-ip>:3001
